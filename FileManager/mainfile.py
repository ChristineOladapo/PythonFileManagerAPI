# mainfile.py

# Christine Oladapo



"""
Entry point for the application.

This module serves as the entry point for the application. It imports 
the main function from the `ui` module and calls it when executed as the main script.

Usage:
    When executed as the main script, this module calls the `main()` 
    function from the `ui` module to start the application.

Example:
    $ python3 main.py
"""
from ui import main
# key 5573155a15275952fe9b7b826eaf957e


if __name__ == "__main__":
    main()
